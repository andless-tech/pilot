# Pilot 0.3.0 验证记录

日期：2026-09-20。属于主机通信与编译验证，**不是实板验收**。

## 2026-09-22 推送前复验

- WSL Ubuntu 22.04 主机测试通过：32 个业务接口、7 类事件、服务断开/重连、
  C++ 头文件和 97 个公共导出符号；HID CLI 5 项测试通过。
- LCD 有界队列及真实服务源码联调通过：写值合并、批次隔离、限频、动作回执、
  弹窗超时、重连与所有者清理；超时注入产生的 `ui error -4` 为预期测试结果。
- 从当前源码重新交叉编译 ARM 静态库、动态库、监测示例与 LCD 示例通过，
  ELF 为 ARM32 小端、EABI5 hard-float，使用 uClibc。
- 主机测试与 LCD 联调的 ASan / UBSan 复验通过；LCD 独立 Makefile 的
  静态、动态链接 ARM 构建通过。
- 本次未进行真机上传、烧录或重新执行历史记录中的全部板端测试。

## 已验证

| 检查项 | 结果 |
| --- | --- |
| 原有 32 项能力的底层消息签名与传输 | 通过 |
| 全部 32 个公共业务函数及返回字段 | 通过 |
| 仅使用公共头文件并动态链接的独立应用：32 个调用及 7 类事件 | 通过 |
| 全部 7 类类型化事件回调、替换、取消与重连恢复 | 通过 |
| 通道/连接/传输统计的业务数组转换 | 通过 |
| 图片/频谱字节引用、属性读取、空数组、64 位精度 | 通过 |
| 超时、权限拒绝、不支持、格式异常和参数错误 | 通过 |
| 非服务所有者的同名广播隔离 | 通过 |
| 服务重启与临时测试总线断开/恢复 | 通过 |
| ASan / UBSan，含回调上下文释放 | 通过 |
| C++11 公共头文件编译 | 通过 |
| 公共头文件及示例不含协议名称、原始消息和通用通信入口 | 通过 |
| 动态库只导出公共头文件声明的 97 个业务/生命周期函数 | 通过 |
| ARM 静态库、动态库、两种链接示例 | 通过 |
| 二进制使用包：排除私有实现，使用包内工具链独立编译静态/动态示例 | 通过 |
| ARM ABI | ELF32 / little endian / EABI5 / hard-float / uClibc |
| LCD 实际服务 + SDK：1000 次写值合并、批次/草稿隔离、前后台限频 | 通过 |
| LCD 操作确认/过期、弹窗 TTL、6 秒失联清理及状态恢复，不重放动作 | 通过 |
| LCD 模型：所有者隔离、UID 配额、原子校验、20000 个损坏/截断包 | ASan / UBSan 通过 |
| LCD 320×172 无头渲染、按键、系统提示抢占及现有回归 | 7/7 通过 |
| LCD 显示程序 ARM 完整链接 | 通过 |

## 复现

在源码工程中：

```sh
make TARGET=host test
make TARGET=host SANITIZE=1 test
make verify
make package
python3 scripts/verify_bundle.py dist/pilot-0.3.0-arm-sdk.tar.gz
make -f tests/ui.mk test-ui SANITIZE=1 DISPLAY_UI=/path/to/display_ui
```

包验证会检查不含私有实现/协议文件，并在临时目录使用包内工具链分别编译静态和动态示例，检查静态应用不依赖 libpilot.so、动态应用依赖 libpilot.so.1。测试只终止自己创建的临时总线，不操作开发板或主机系统总线。

## 限制

- 未上传或执行开发板程序，未验证真实传感器数据、权限、配置持久化或运行负载。
- 模拟服务只验证客户端通信及数据转换，不替代板端业务验证。
- LCD 联调使用真实服务源码，但运行在主机临时总线上；不证明板端系统总线策略、CPU/RSS、长时稳定性与物理屏幕效果。
- 静态 Pilot 仍依赖板端已有基础动态库，不是全静态可执行文件。
- 封装不提供协议保密、应用沙箱或安全隔离，也不会改变已经公开的仓库历史。

## 2026-09-21 单程序部署增量验证

- HID 传输、身份注册原有测试通过；新单程序传输测试通过。
- 验证 1 MiB 成功、1 MiB + 1 拒绝、固定目的名、错误 CRC/SHA/ELF、
  重复分片/提交、未完成提交、取消、超时和原子覆盖；失败保留旧文件。
- 传输与日志环形缓存 ASan/UBSan 通过，精确保留最后 1024 字节。
- 主机运行监管测试通过：开机启动、nice19、32 MiB 地址空间、
  清空 capabilities、no_new_privs、禁止 fork、允许 pthread、
  停止/重启、TERM 后 KILL、监管崩溃结束子进程、三次退出熔断。
- 主机测试与其他工具共享 UID，所以仅测试版将任务上限调到 1024
  来验证 pthread；正式构建为独立 UID、16 任务，禁止启用该测试覆盖项。
- rootfs 账户创建幂等、保留原账户、UID 冲突拒绝和 init 脚本语法验证通过。
- 主机 CLI 5 项测试通过：双向分片、错误回执、边界长度、超限先拒绝、
  固定路径上传及默认不重启。
- ARM hid_agent/pilot-runner 完整链接通过；SDK 使用包包含 CLI、
  中文部署说明，包内工具链静态/动态示例独立编译通过。
- **尚未进行真机 HID 上传、掉电持久化、ARM 上权限/线程运行、
  SDK 在专用用户下调用业务能力或长时资源测试；未构建或烧录完整固件。**
